mocha = require 'mocha'
chai = require 'chai'
should = chai.should
fixtures = require './fixtures'

jsdom = require 'jsdom'

champion = require '../Champion'
championNames = require '../lib/championsNames'

describe 'champion-scraper', ->
  describe 'index', ->
  describe 'Champion', ->
    it 'should ', ->

  it 'should be able to a parse out a champion from the url', ->

    result.should.equal fixtures.Ahri
